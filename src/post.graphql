mutation CreatePost($post: PostInput) {
  createPost(post: $post) {
    id
    title
  }
}

query GetPosts {
  getPosts {
    id
    title
    description
    file_
    tags {
      type
      lube
      film
    }
    createdAt
    author {
      username
      id
    }
  }
}
